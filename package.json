{
  "name": "rss-pages",
  "version": "1.0.0",
  "scripts": {
    "fast-update": "node index.js & git add common & git add site & git add dist & git add index.js && git commit -m update && git push"
  },
  "dependencies": {
    
  }
}
